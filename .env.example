# Database
DATABASE_URL="postgresql://neondb_owner:npg_ZCiwvNWX2jx7@ep-bitter-night-aqtwyjc3-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Redis
REDIS_URL="redis://localhost:6379"

# Auth — generate with: openssl rand -base64 32
JWT_SECRET="replace-with-secure-secret-min-32-chars"

# Encryption — generate with: openssl rand -base64 32
ENCRYPTION_KEY="replace-with-32-byte-base64-key"

# App
APP_URL="http://localhost:3000"
NODE_ENV="development"
