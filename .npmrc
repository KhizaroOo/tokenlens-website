# Enable Windows long-path support to prevent partial package installs.
# Some packages (e.g. lucide-react ESM icons) have deep directory trees that
# exceed Windows MAX_PATH (260 chars). Without this flag, npm silently skips
# files beyond the limit, causing incomplete installs that fail at runtime.
long-path=true
